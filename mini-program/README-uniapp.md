# uniapp 小程序项目

这个项目已经从 Vue.js 项目转换为 uniapp 架构，可以同时开发 H5 和微信小程序。

## 🚀 开发方式

### 推荐方式：使用 HBuilderX（官方推荐）

1. **下载 HBuilderX**
   - 访问：https://www.dcloud.net.cn/hbuilderx.html
   - 下载并安装 HBuilderX

2. **导入项目**
   - 打开 HBuilderX
   - 选择 "文件" -> "导入" -> "从本地目录导入"
   - 选择此项目根目录

3. **运行项目**
   - 在项目管理器中右键项目
   - 选择 "运行到浏览器" 或 "运行到小程序模拟器"
   - 或者使用菜单：运行 -> 运行到浏览器/小程序

## 📁 项目结构

```
mini-program/
├── src/                    # 源代码目录
│   ├── components/         # 组件
│   ├── pages/             # 页面（转换自 views/）
│   ├── static/            # 静态资源
│   ├── utils/             # 工具函数
│   ├── App.vue            # 应用入口
│   └── main.js            # 主入口文件
├── pages.json             # 页面配置（替代 Vue Router）
├── manifest.json          # 应用配置
├── uni.scss              # 全局样式
└── package.json          # 项目配置
```

## 🛠 命令行方式

现在支持完整的命令行开发方式：

### 开发命令

```bash
# H5 开发 - 启动本地服务器
npm run dev:h5
# 访问: http://localhost:8080

# 微信小程序开发 - 显示项目路径供微信开发者工具导入
npm run dev:mp-weixin

# H5 构建
npm run build:h5

# 微信小程序构建
npm run build:mp-weixin
```

### 使用说明

1. **H5 开发**：直接在浏览器中运行，支持热重载
2. **微信小程序**：使用微信开发者工具导入项目目录进行开发
3. **构建**：生成生产版本的代码

**优势：** 无需额外 IDE，完全使用命令行工具。

## 📋 主要变更

### 1. 架构转换
- 从 Vue 3 + Vite 转换为 uniapp Vue 2
- 使用 `pages.json` 替代 Vue Router
- 使用 uniapp API 替代浏览器 API

### 2. 文件结构调整
- `views/` → `pages/`
- `public/` → `static/`
- 新增 `manifest.json` 和 `pages.json`

### 3. 组件适配
- Vue 3 Composition API → Vue 2 Options API
- Vue Router → uniapp 页面跳转 API
- localStorage → uni.getStorageSync

### 4. 样式系统
- 移除 Tailwind CSS
- 使用 uni.scss 全局样式
- 适配移动端样式

## 🔧 API 变更对照

| 原 API | uniapp API |
|--------|------------|
| `this.$router.push()` | `uni.navigateTo()` |
| `localStorage.getItem()` | `uni.getStorageSync()` |
| `axios.get()` | 自定义 request 工具 |
| `window.location` | `uni.getEnterOptionsSync()` |

## 🎯 下一步

1. 使用 HBuilderX 打开项目
2. 运行到浏览器或小程序模拟器进行测试
3. 根据需要调整样式和功能
4. 发布到相应平台

## 📞 技术支持

- uniapp 官方文档：https://uniapp.dcloud.net.cn/
- HBuilderX 文档：https://hx.dcloud.net.cn/
- DCloud 社区：https://ask.dcloud.net.cn/
